# Post-hackathon TODO

- Rotate all API keys (they're exposed in env to anyone who cloned)
- Move Anthropic + OpenAI + ASI1 calls behind a Supabase Edge Function or Next.js API route with auth
- Add rate limiting on /api/process
- Add artisan auth (phone number + SMS code)
- Real SMS order notifications (Twilio)
- Replace direct main pushes with feature branch workflow

# Things that went wrong / lessons
(fill in as the day goes)
